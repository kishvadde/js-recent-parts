var name = "kishore";
var age = 25

function upper(strings, ...values){
    return ""
}

// tagged string. function gets called immediatly
// with strings as splitted by spaces and values as provieded variables
var msg = upper`HI, ${name}. you are ${age} years old.`;

//string functions

st.padStart(lengthToPad, "padding chars") //
st.padEnd(lengthToPad, "padding chars")

st.trim() //trims on both sides
st.trimStart() //trim at the begining
st.trimEnd() //trim at the end

